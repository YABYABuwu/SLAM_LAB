# แนวทางทำงานในโปรเจคนี้

โปรเจคนี้ต่อยอดจาก `YABYABuwu/Template_robomaster_project` สำหรับ RoboMaster EP อ่าน [ภาพรวมระบบ](docs/ARCHITECTURE.md) และ [แนวทางเพิ่มระบบ](docs/EXTENDING.md) ก่อนแก้โค้ด

## หลักการแก้ไข

1. ตรวจเส้นทางข้อมูลและความสามารถเดิมก่อนเพิ่มโมดูล: `config/settings.yaml` → `src/config_loader.py` → `main.py` → `src/logger.py` / `src/chassis.py` → `src/dashboard.py` และ `src/run_review.py`
2. ใช้ `SensorLogger` เป็นแหล่งข้อมูลเซนเซอร์ร่วม และใช้ `ChassisController` สำหรับคำสั่งเคลื่อนที่ที่รองรับอยู่แล้ว หากต้องใช้โครงสร้างใหม่ ให้ระบุเหตุผลว่าของเดิมไม่รองรับตรงไหนและกำหนดวิธีเชื่อมกลับเข้าระบบหลัก
3. ทุกฟีเจอร์ที่มีสถานะหรือข้อมูลสำคัญต่อผู้ควบคุม ต้องตรวจและปรับ dashboard สดให้สอดคล้อง (`src/dashboard.py`, `dashboard/index.html`) ถ้าบันทึกข้อมูลหรือเหตุการณ์ที่ควรตรวจย้อนหลัง ต้องตรวจและปรับหน้า review (`src/run_review.py`, `review/index.html`) ด้วย
4. รักษาหน่วย พิกัด อายุข้อมูล และวงจร `start`/`stop` ให้ชัดเจน ห้ามเปิด SDK subscription ซ้ำเพื่ออ่านข้อมูลชุดเดียวกันโดยไม่มีเหตุผล
5. ปรับ config, การตรวจ config, เอกสาร และการทดสอบที่เกี่ยวข้องพร้อมกันเมื่อเปลี่ยนพฤติกรรมหรือสัญญาข้อมูล
6. ทดสอบ logic ด้วย `python3 -m unittest discover -s tests -v` การทดสอบด้วยหุ่นยนต์จริงยังต้องทำแยกเมื่อมีการเปลี่ยนส่วนฮาร์ดแวร์หรือการเคลื่อนที่
7. ระบบ DFS/SLAM ต้องใช้ `SensorLogger` ร่วม, ปฏิเสธข้อมูล pose/ToF ที่ไม่สดหรือ scan ไม่ครบ, ตรวจระยะ/แผนที่ก่อนเดินและก่อนถอยกลับ และคง `exploration.enabled: false` เป็นค่าเริ่มต้นจนสอบเทียบตำแหน่ง ToF จริง
8. รักษา format/version ของ `robomaster-occupancy-grid` ให้ export/import เข้ากัน และอัปเดต canvas กับปุ่มโหลด/ส่งออกบน dashboard เมื่อเปลี่ยนแผนที่

รายละเอียดจุดเชื่อมและ checklist อยู่ใน `docs/EXTENDING.md` อย่าสรุปว่า dashboard รองรับฟีเจอร์ใหม่เพียงเพราะกราฟทั่วไปสร้างอัตโนมัติ ต้องตรวจว่าผู้ใช้เห็นสถานะ หน่วย และเหตุผิดปกติที่จำเป็นจริง
